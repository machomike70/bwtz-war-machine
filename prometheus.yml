global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'gateway'
    static_configs:
      - targets: ['gateway-api:8000']

  - job_name: 'orchestrator'
    static_configs:
      - targets: ['agent-orchestrator:8001']

  # Add more services as they gain /metrics endpoints in later phases